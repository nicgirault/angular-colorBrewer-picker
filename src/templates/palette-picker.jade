ui-select(
  ng-model="selectedPalette"
  style="width: 300px;"
)
  ui-select-match(placeholder="Select a palette...")
    palette(colors='selectedPalette.range[range]' size='20')
  ui-select-choices(repeat='palette in palettes')
    palette(colors='palette.range[range]' size='20')
